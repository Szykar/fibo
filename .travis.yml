language: node_js
node_js:
  - 'node'
cache:
  yarn: true
  directories:
    - public
    - .cache
script:
  - yarn build

deploy:
  provider: pages
  github-token: $GITHUB_TOKEN
  skip-cleanup: true
  local_dir: dist
  keep-history: true
  target_branch: gh-pages
  on:
    branch: master
